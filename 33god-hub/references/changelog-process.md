# Changelog Process

Pipeline-wide changes require both:

- A machine entry in `33god-platform/changes/*.jsonl`.
- A human summary in `33god-platform/CHANGELOG.pipeline.md`.

Run `python3 scripts/platform.py validate` before treating the change as ready.

"""activity-report: periodic project updates for one declared audience.

`__version__` is the skill version stamped into every emitted event as
`data.generator.skill_version`; keep it equal to `metadata.version` in SKILL.md.
"""

__version__ = "0.1.2"

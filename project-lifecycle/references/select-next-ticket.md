# Select Next Ticket

Compare the requested board's ready work against dependencies, stated priority,
and current progress. Explain the recommended issue and blockers. A selection
recommendation does not authorize starting implementation, changing its state,
creating a swarm, or broadcasting a notification. When the user requests work,
follow that scope and the existing project lifecycle.

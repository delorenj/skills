# Create Ticket From Story

Use an explicitly supplied story or authorized triage result. Resolve the
project board and existing matching issue before creating anything. Preserve
acceptance criteria and dependencies. Use board-taxonomy for labels/states.
Do not recursively convert every paragraph into tickets or notify other people
without authorization. Report the actual issue identifier and mutation.

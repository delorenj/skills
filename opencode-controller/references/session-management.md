## Session rules

- OpenCode keeps project session history.
- Reuse the existing project session whenever present.
- Reusing sessions preserves context and decisions.
- Create a new session only if the user explicitly asks or no project session exists.
- Open the session selector with:
  `/sessions`

If unsure whether a displayed session matches the project, ask the user before creating a new one.
